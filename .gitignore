# Never, ever commit API keys, passwords, or configuration profiles to GitHub.
.env
*.env
config.json
credentials.json

# Keeps your repo clean. No need to upload thousands of local library source files.
venv/
.venv/
env/
ENV/

# Hidden system files created by your computer's file explorer.
.DS_Store
Thumbs.db
desktop.ini

# Temporary files generated by Python when running your scripts.
**/__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/

# Prevents uploading massive financial PDFs or local sqlite/vector databases.
data/*.pdf
data/*.db
*.local

# Local blueprint to maintain project architecture and progress
# Feed back to any AI assistant being used so that context is maintained
context_blueprint.txt
Agentic_RAG.excalidraw